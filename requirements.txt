# Postgres driver. build_db/refresh/export need this only when CK_DB_URL is a
# postgresql:// URL; the SQLite path has no third-party dependency at all.
psycopg[binary]>=3.1

# build_geo.py only
shapely>=2.0
openpyxl>=3.1

# serve.py only - the read-only HTTP UI + JSON API
fastapi>=0.110
uvicorn[standard]>=0.27
